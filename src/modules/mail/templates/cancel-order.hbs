<!DOCTYPE html>
<html lang="en" xmlns:v="urn:schemas-microsoft-com:vml">

<head>
    <meta charset="utf-8">
    <meta name="x-apple-disable-message-reformatting">
    <meta http-equiv="x-ua-compatible" content="ie=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="format-detection" content="telephone=no, date=no, address=no, email=no">
    <meta name="color-scheme" content="light dark">
    <meta name="supported-color-schemes" content="light dark">
    <title>The MALT STORE - Hủy đơn hàng</title>
    <style>
        body {
            word-break: break-word;
            margin: 0;
            width: 100%;
            background-color: #f8fafc;
            padding: 0;
            font-family: Arial, sans-serif;
        }

        .container {
            width: 100%;
            max-width: 600px;
            margin: 0 auto;
            background-color: #ffffff;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            padding: 20px;
        }

        .header {
            background-color: #c0392b;
            color: white;
            padding: 20px;
            text-align: center;
            position: relative;
        }

        .header h1 {
            margin: 0;
            font-size: 24px;
            font-weight: bold;
        }

        .header img {
            position: absolute;
            right: 20px;
            top: 20px;
            width: 50px;
            height: 50px;
        }

        .header p {
            margin: 0;
            font-size: 14px;
            font-weight: lighter;
        }

        .content {
            padding: 20px;
        }

        .content h3 {
            color: #333333;
        }

        .content h4 {
            color: #c0392b;
        }

        .product {
            display: flex;
            align-items: center;
            border-bottom: 1px solid #eeeeee;
            padding: 10px 0;
        }

        .product img {
            max-width: 100px;
            border-radius: 8px;
            margin-right: 20px;
        }

        .product-details {
            flex: 1;
        }

        .product-details li {
            margin-bottom: 5px;
        }

        .footer {
            background-color: #f9f9f9;
            color: #666666;
            padding: 20px;
            text-align: center;
        }

        .footer p {
            margin: 0;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="header">
            <h1>Đơn hàng của bạn đã bị hủy</h1>
            <p>Chúng tôi rất tiếc vì sự bất tiện này</p>
            <img src="https://img.icons8.com/clouds/100/000000/cancel.png" alt="Cancel Order">
        </div>
        <div class="content">
            <h3>Chi tiết đơn hàng đã bị hủy của bạn:</h3>
            <div>
                <h4>&rsaquo; Danh sách sản phẩm</h4>
                {{#each data.orderDetails}}
                <div class="product">
                    <img class="product-image" src="{{this.productStore.product.image}}" alt="{{this.productStore.product.name}}">
                    <ul class="product-details">
                        <li>Tên sản phẩm: <b>{{this.productStore.product.name}}</b></li>
                        <li>Số lượng: <b>{{this.quantity}}</b></li>
                        <li>Giá của sản phẩm: <b>{{this.productStore.product.price}}</b></li>
                    </ul>
                </div>
                {{/each}}
            </div>
            <div>
                <h4>&rsaquo; Thông tin đơn hàng</h4>
                <ul>
                    <li>Tên: <b>{{data.metadata.Information.lastName}} {{data.metadata.Information.firstName}}</b></li>
                    <li>Số điện thoại: <b>{{data.metadata.Information.phoneNumber}}</b></li>
                    <li>Địa chỉ: <b>{{data.metadata.Information.address}}</b></li>
                    <li>Phương thức thanh toán: <b>{{data.paymentMethod}}</b></li>
                    <li>Giá trị đơn hàng: <b>{{data.total}}</b></li>
                    <li>Phí vận chuyển: <b>{{data.shipping}}</b></li>
                    {{#if data.voucher}}
                    <li>Voucher giảm giá: <b>{{data.voucherValue}}</b></li>
                    {{/if}}
                    <li>Tổng giá trị đơn hàng: <b>{{data.totalPrices}}</b></li>
                </ul>
            </div>
        </div>
        <div class="footer">
            <p>&copy; 2024 The MALT STORE. All rights reserved.</p>
        </div>
    </div>
</body>

</html>
